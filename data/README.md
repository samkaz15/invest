# data/

取得した生ペイロードの永久保存先（ADR-010）。

```
data/raw/<source_id>/<YYYY-MM>/<raw_item_id>.json
data/raw/<source_id>/_hashes.txt      # 重複排除の台帳
```

**このディレクトリはコミットする。** データベースを失っても、
ここから正規化をやり直せば再構築できる。逆はできない。

生ペイロードは取得したまま保存し、加工しない。
抽出ロジックを直したら、過去の生データに対して再実行すればよい。
