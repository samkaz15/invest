source_id: src_fred_cpimedsl
name: FRED CPIMEDSL — US CPI medical care
kind: http_json
url: https://api.stlouisfed.org/fred/series/observations?series_id=CPIMEDSL&api_key=${FRED_API_KEY}&file_type=json&sort_order=desc&limit=400
tier: 1
min_interval_seconds: 2
notes: >-
  Feeds ser_us_cpi_medical. Requires FRED_API_KEY (free); the source is auto-disabled and
  reported as a data gap until the key is set.
