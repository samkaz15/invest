source_id: src_fred_cusr0000seta02
name: FRED CUSR0000SETA02 — US CPI used cars and trucks
kind: http_json
url: https://api.stlouisfed.org/fred/series/observations?series_id=CUSR0000SETA02&api_key=${FRED_API_KEY}&file_type=json&sort_order=desc&limit=400
tier: 1
min_interval_seconds: 2
notes: >-
  Feeds ser_us_cpi_used_cars. Requires FRED_API_KEY (free); the source is auto-disabled and
  reported as a data gap until the key is set.
